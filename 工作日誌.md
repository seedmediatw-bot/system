# 工作日誌

---

## 2026-05-08

### HR 自動化系統（index.html + Cloudflare Worker）

#### 1. 報帳頁 ── 發票辨識功能（雙模式）
- 新增「收據 / 發票」區塊，支援兩種模式切換：
  - **一般模式**：即時拍照（`capture="environment"` 支援手機鏡頭）或從相簿選取，照片縮圖即時顯示於清單，可逐張刪除
  - **AI 辨識模式**：選取多張發票照片，送至 Gemini API 自動辨識，回傳 JSON 後自動填入費用明細（說明、類別、金額、日期）
- 辨識結果會疊加到已有明細，不會覆蓋手動輸入的項目

#### 2. 報帳頁 ── 收據圖片直傳 Notion
- 送出報帳時，一般模式的照片透過 Cloudflare Worker `/api/notion-upload` 端點上傳：
  1. `POST /v1/files` 向 Notion 申請上傳位置
  2. `PUT` 二進位資料到 Notion 簽署 URL
  3. 取得 `file_upload_id`，以 Image Block 附加到報帳頁面
- 圖片直接可在 Notion 頁面中預覽，不需外部連結

#### 3. 報帳送出 Bug 修正
- 移除 `buildExpenseProps` 中的 `收據連結` URL 欄位（該欄位不存在於 Notion DB，導致送出失敗）
- 補寫原本缺漏的兩個欄位：`員工編號`、`備註`（備註同時合併收據連結文字）

#### 4. Gemini API 金鑰安全性
- 移除前端 `localStorage` 儲存 Gemini API Key 的機制
- Gemini Key 改由 Cloudflare Worker 的環境變數（`GEMINI_API_KEY`）統一管理，前端不再暴露金鑰

#### 5. Cloudflare Worker 全面改版（user 主導）
- 使用者自行改寫 `cloudflare-worker.js`，以 Gemini API 取代 Claude API
- 合併最終版本：保留 `/api/notion-upload` 端點、新增 `/api/gemini` 支援動態 `model` 參數與 `x-gemini-key` header 備援、加回 404 fallback
- `/api/status` 改回傳 `gemini` 狀態欄位（取代 `claude`）
- CORS headers 新增 `PUT` method 與 `x-gemini-key`

#### 6. AI 助理改接 Gemini
- `sendAI()` 從呼叫 `/api/claude`（Claude Messages 格式）改為呼叫 `/api/gemini`（Gemini `contents/systemInstruction` 格式）
- 回應解析路徑更新為 `candidates[0].content.parts[0].text`

#### 7. Notion 報帳 DB 欄位比對
- 核查 Notion「💰 報帳申請」DB schema，確認所有程式碼寫入欄位均有對應
- 補寫缺漏欄位 `員工編號`（rich_text）與 `備註`（rich_text）

---

### 一頁式網站（Claude Artifacts）
- 在 Claude.ai 的 Artifacts 功能上設計並產出一頁式網站原型
- 作為獨立展示頁或行銷落地頁使用

---

### Git 提交紀錄（2026-05-08）

| commit | 說明 |
|---|---|
| `aed0bc4` | 報帳表單補寫員工編號與備註欄位至 Notion |
| `ea94f87` | AI 助理改用 Gemini API，cloudflare-worker.js 移除 Claude 端點 |
| `b987ec3` | 報帳收據圖片直傳 Notion：拍照後自動上傳並附加為頁面 Image Block |
| `d07d728` | 修正報帳送出失敗：移除不存在的收據連結欄位 |
| `fc8f180` | 更新教學文件並新增教學文件模板 |
| `d95c898` | 報帳頁新增發票辨識功能（雙模式） |

---

## 2026-05-11

### CRM 與 HR 系統全面整合 (Transplantation & Integration)

#### 1. CRM 模組完整移植至 HR 系統 (index.html)
- 將原本獨立的 `crm.html` (自動工作 CRM) 完整移植進 `index.html` (HR 自動化系統)。
- **介面整合**：
  - 在 HR 側邊欄新增「客戶關係管理」專屬區塊，包含：客戶總覽、新增客戶、更新客戶、拜訪記錄。
  - 補齊了對應的 HTML 頁面結構 (`page-crm-dashboard` 等)，修復了點擊選單出現白畫面的問題。
- **邏輯遷移**：
  - 遷移所有 CRM 專屬 JavaScript 函式（如 `loadClients`, `analyzeAdd`, `submitCall` 等）。
  - 修正 ID 衝突與變數命名，確保 CRM 功能與原有的 HR 功能（請假、報帳）互不干擾。

#### 2. 統一 Notion 資料庫管理與設定
- **設定頁面升級**：在「Notion 設定」頁面新增 CRM 客戶與拜訪記錄的資料庫 ID 填寫欄位。
- **變數收斂**：將 CRM 原本獨立的 `CRM_DB`、`CALL_DB` 變數統一併入 HR 系統的 `nDBs` 物件管理，並同步儲存於 `localStorage`。
- **授權導引**：協助確認 Notion Database 的「Connect to」授權設定，解決連線 404/403 錯誤。

#### 3. 程式碼優化與修復
- **錯誤處理**：優化 `loadClients` 與 API 呼叫的報錯提示，能顯示具體 HTTP 錯誤碼（如 401, 404）以便排查。
- **字串清理**：修復了因代碼貼上導致 HTML 中出現字面值 `\n` 字串的渲染異常。
- **導覽邏輯**：修正 `goUpdate()` 函式，使其能正確切換至整合後的 `crm-update` 頁面。

---
